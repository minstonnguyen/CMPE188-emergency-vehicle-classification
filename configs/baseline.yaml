paths:
  data_dir: data/processed
  model_dir: models
  output_dir: outputs

training:
  image_size: 128
  batch_size: 16
  epochs: 5
  learning_rate: 0.001
  seed: 42
  num_workers: 0
  device: auto
